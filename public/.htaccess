<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # File e cartelle reali in public/
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # React Router — tutte le route client-side
  RewriteRule ^ index.html [L]
</IfModule>
